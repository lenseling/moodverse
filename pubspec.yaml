name: moodverse
description: PearlHacks 2024 Project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter

  chat_gpt_sdk: ^3.0.1

  english_words: ^4.0.0
  provider: ^6.0.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo-transparent.png
    - assets/astrobear.png
